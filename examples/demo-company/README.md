# Demo company fixtures

Use `tasks.csv` to verify stable CSV upserts and Japanese hybrid retrieval. `ADR-001-context-boundary.md` exercises Markdown normalization and source-linked document search. Re-importing either file must update by stable ID/checksum without duplicating a document or chunk.
